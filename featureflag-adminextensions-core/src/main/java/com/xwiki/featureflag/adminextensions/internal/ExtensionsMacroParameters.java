package com.xwiki.featureflag.adminextensions.internal;

/**
 * Empty parameters class for the extensions macro.
 * Required to avoid IllegalAccessException when XWiki tries to instantiate parameters.
 */
public class ExtensionsMacroParameters {
    // No properties — macro has no parameters
}